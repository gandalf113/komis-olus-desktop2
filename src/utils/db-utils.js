/**
 * Performs operations in the database
 * @param {string} method - ["get", "create"] operation type
 * @param {string} table - SQLite table on which we're operating
 * @param {string} searchColumn - column in which we're searching for searchValue
 * @param {string} searchValue - value by which we're filtering
 */
export const sendRequest = (method, table, searchColumn = undefined, searchValue = undefined) => {

}